import {InfinitScrollDirective} from './infinit-scroll.directive';

describe('InfinitScrollDirective', () => {
    it('should create an instance', () => {
        const directive = new InfinitScrollDirective();
        expect(directive).toBeTruthy();
    });
});
